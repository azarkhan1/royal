# 🛍️ فروشگاه آنلاین - E-commerce Website

یک فروشگاه آنلاین کامل و حرفه‌ای با استفاده از HTML, CSS, JavaScript و Node.js/Express با پشتیبانی از MySQL.

## ✨ ویژگی‌ها

- ✅ **سه صفحه اصلی**: صفحه اصلی، لیست محصولات، سبد خرید و تسویه حساب
- ✅ **نمایش محصولات**: با تصویر، قیمت و دکمه افزودن به سبد
- ✅ **سبد خرید**: مدیریت کامل سبد خرید با امکان تغییر تعداد و حذف
- ✅ **فرم تسویه حساب**: با اعتبارسنجی کامل و ارسال به واتساپ
- ✅ **پنل مدیریت**: برای مدیریت محصولات و سفارشات
- ✅ **پشتیبانی از زبان فارسی**: تمام متن‌ها به فارسی و RTL
- ✅ **طراحی ریسپانسیو**: سازگار با موبایل و تبلت
- ✅ **امنیت**: محافظت در برابر XSS و SQL Injection
- ✅ **اتصال به دیتابیس MySQL**: برای ذخیره محصولات و سفارشات

## 📋 پیش‌نیازها

- Node.js (نسخه 14 یا بالاتر)
- MySQL (نسخه 5.7 یا بالاتر)
- npm یا yarn

## 🚀 نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
npm install
```

### 2. تنظیم دیتابیس MySQL

1. MySQL را راه‌اندازی کنید
2. فایل `database.sql` را در MySQL اجرا کنید:

```bash
mysql -u root -p < database.sql
```

یا از طریق phpMyAdmin یا هر ابزار مدیریت MySQL دیگر فایل را import کنید.

### 3. تنظیمات دیتابیس (اختیاری)

اگر تنظیمات دیتابیس شما متفاوت است، می‌توانید متغیرهای محیطی را تنظیم کنید:

```bash
# Windows (PowerShell)
$env:DB_HOST="localhost"
$env:DB_USER="root"
$env:DB_PASSWORD="your_password"
$env:DB_NAME="ecommerce_db"

# Linux/Mac
export DB_HOST="localhost"
export DB_USER="root"
export DB_PASSWORD="your_password"
export DB_NAME="ecommerce_db"
```

یا فایل `server.js` را ویرایش کنید و تنظیمات را در متغیر `dbConfig` تغییر دهید.

### 4. راه‌اندازی سرور

```bash
npm start
```

یا برای حالت توسعه (با auto-reload):

```bash
npm run dev
```

سرور در آدرس `http://localhost:3000` اجرا می‌شود.

## 📁 ساختار فایل‌ها

```
.
├── index.html          # صفحه اصلی
├── products.html       # صفحه لیست محصولات
├── cart.html           # صفحه سبد خرید و تسویه حساب
├── admin.html          # پنل مدیریت
├── style.css           # فایل استایل (CSS)
├── script.js           # فایل JavaScript (Frontend)
├── server.js           # سرور Node.js/Express (Backend)
├── database.sql         # ساختار دیتابیس MySQL
├── package.json         # وابستگی‌های Node.js
└── README.md           # این فایل
```

## 🎯 استفاده

### صفحات اصلی

1. **صفحه اصلی** (`index.html`):
   - نمایش دسته‌بندی‌ها
   - محصولات ویژه
   - آخرین محصولات

2. **صفحه محصولات** (`products.html`):
   - لیست تمام محصولات
   - فیلتر بر اساس دسته‌بندی و قیمت
   - جستجو در محصولات
   - مرتب‌سازی

3. **صفحه سبد خرید** (`cart.html`):
   - مشاهده محصولات در سبد
   - تغییر تعداد
   - حذف محصول
   - تسویه حساب و ارسال به واتساپ

### پنل مدیریت

برای دسترسی به پنل مدیریت:
1. به آدرس `http://localhost:3000/admin.html` بروید
2. نام کاربری: `admin`
3. رمز عبور: `admin123`

**امکانات پنل مدیریت:**
- مشاهده و مدیریت محصولات
- مشاهده و مدیریت سفارشات
- افزودن محصول جدید
- تغییر وضعیت سفارشات

## 📱 یکپارچه‌سازی واتساپ

پس از ثبت سفارش، اطلاعات سفارش به صورت خودکار به واتساپ ادمین ارسال می‌شود.

**تنظیم شماره واتساپ:**
در فایل `script.js`، خط مربوط به `adminPhone` را پیدا کرده و شماره خود را وارد کنید:

```javascript
const adminPhone = '989123456789'; // فرمت: 989123456789 (بدون +)
```

**نکته:** شماره باید به فرمت بین‌المللی و بدون + باشد (مثال: 989123456789).

## 🔒 امنیت

- **XSS Protection**: تمام ورودی‌های کاربر پاکسازی می‌شوند
- **SQL Injection Protection**: استفاده از prepared statements
- **Input Validation**: اعتبارسنجی کامل فرم‌ها
- **CORS**: تنظیم شده برای امنیت بیشتر

## 🌐 API Endpoints

### محصولات

- `GET /api/products` - دریافت همه محصولات
- `GET /api/products/:id` - دریافت یک محصول
- `POST /api/products` - افزودن محصول (ادمین)
- `PUT /api/products/:id` - به‌روزرسانی محصول (ادمین)
- `DELETE /api/products/:id` - حذف محصول (ادمین)

### سفارشات

- `GET /api/orders` - دریافت همه سفارشات (ادمین)
- `GET /api/orders/:id` - دریافت یک سفارش
- `POST /api/orders` - ثبت سفارش جدید
- `PUT /api/orders/:id/status` - تغییر وضعیت سفارش (ادمین)

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها

در فایل `style.css`، متغیرهای CSS را ویرایش کنید:

```css
:root {
    --primary-color: #4a90e2;
    --secondary-color: #50c878;
    --danger-color: #e74c3c;
    /* ... */
}
```

### افزودن دسته‌بندی جدید

1. در فایل `database.sql` دسته‌بندی جدید را اضافه کنید
2. در فایل‌های HTML و JavaScript دسته‌بندی را اضافه کنید

## 🐛 عیب‌یابی

### مشکل اتصال به دیتابیس

اگر سرور بدون دیتابیس اجرا می‌شود:
- بررسی کنید MySQL در حال اجرا باشد
- اطلاعات اتصال را بررسی کنید
- دیتابیس را ایجاد کرده باشید

### محصولات نمایش داده نمی‌شوند

- بررسی کنید API در حال اجرا باشد (`http://localhost:3000/api/products`)
- Console مرورگر را برای خطاها بررسی کنید
- در صورت عدم اتصال به دیتابیس، از داده‌های نمونه استفاده می‌شود

## 📝 یادداشت‌ها

- در صورت عدم اتصال به دیتابیس، سرور از داده‌های نمونه استفاده می‌کند
- سبد خرید در LocalStorage مرورگر ذخیره می‌شود
- برای استفاده در production، باید تنظیمات امنیتی بیشتری اعمال شود

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 👨‍💻 توسعه

برای توسعه بیشتر:
- می‌توانید سیستم احراز هویت واقعی اضافه کنید
- پرداخت آنلاین را یکپارچه کنید
- سیستم امتیازدهی و نظرات را اضافه کنید
- پنل مدیریت را گسترش دهید

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال، لطفاً issue ایجاد کنید.

---

**ساخته شده با ❤️ برای فروشگاه‌های آنلاین فارسی**



